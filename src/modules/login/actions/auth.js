import constants from "../constants";
import {push} from "redux-router";
import config from "../../config";
import {checkHttpStatus, parseJSON} from "../../common/utils";
import {get} from "../../common/actions/HTTPmethods";

//let {LOGIN_USER_SUCCESS, LOG_OUT, CLEAN_REDUCER_DATA, SET_REDIRECT_URL_PATH} = constants;

//export function userLoggedIn(username, userObject, role, token){

    // return {
    //     type: LOGIN_USER_SUCCESS,
    //     payload: {
    //         username: username,
    //         userObject: userObject,
    //         role: role,
    //         token: token
    //     }
    // }

//}
